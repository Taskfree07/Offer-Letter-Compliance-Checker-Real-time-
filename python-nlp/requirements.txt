flask
flask-cors
spacy
gunicorn
python-dotenv
python-docx
requests
docxtpl
PyPDF2  # Basic PDF support (lightweight, needed by pdf_service)
# gliner  # Heavy AI model - excluded from production
# PyMuPDF  # PDF library - excluded from production  
# pdfplumber  # PDF library - excluded from production
# weasyprint  # HTML to PDF - excluded from production
