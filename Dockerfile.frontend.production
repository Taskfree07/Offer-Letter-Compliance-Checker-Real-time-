FROM node:18-alpine

WORKDIR /app

# Copy the already-built production files
COPY build/ ./build/

# Install serve to run the production build
RUN npm install -g serve

EXPOSE 3000

# Serve the production build
CMD ["serve", "-s", "build", "-l", "3000"]
